# tifbert (stub)

TifBERT: [mohsenh17/TifBERT](https://github.com/mohsenh17/TifBERT).

Not yet reimplemented. [`paper.md`](paper.md) records what the original work
did — model, data, evaluations and published numbers — as the reference for
the reimplementation and for which evaluations `reimp_shared.eval` adopts.
When code lands, this directory becomes a workspace member like `txfm/`.
